# Internal — never ships, and this repo is PUBLIC (tier doc §2).
# PRODUCT.md carries the sourcing trail, the "must NOT claim" list and the pitch.
PRODUCT.md
work/
photos_raw/

# Derived — regenerate with ./package.sh and `npm run preview`
deploy/
preview.artifact.html
preview.prices.html
*.zip

# Tooling for the verification gate only; the site itself has no build step.
node_modules/
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
